
## Suggestions
* Use adaptive model

## Known Issues
* The webcam can't handle user override, add basic handling of this (model doesn't change but the brightness won't change until its returned to model's expected levels)
